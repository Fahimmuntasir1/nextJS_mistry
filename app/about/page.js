import React from 'react';

const aboutPage = () => {
    // throw new error('There are an Error in this application')
    return (
        <div>
            <h1>This is about page</h1>
        </div>
    );
};

export default aboutPage;